# Public local defaults, not production credentials.
RELAY_ADMIN_TOKEN=local-relay-admin-token-change-me-now
POSTGRES_PASSWORD=relay-local-only
RELAY_PORT=8080
# Leave empty for $0 mock-only development.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
COHERE_API_KEY=
