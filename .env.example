# Public LOCAL DEMO defaults, not production credentials.
RELAY_ADMIN_TOKEN=local-demo-admin-token-change-before-sharing
POSTGRES_PASSWORD=relay-local-only
RELAY_PORT=8080
# Leave empty for $0 mock-only development.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
COHERE_API_KEY=
